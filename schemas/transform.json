{
    "@context": {
        "@version": 1.1,
        "@protected": true,
        "lsreshape": "https://lschema.org/reshape/",

        "if":  "lsreshape:if",
        "export": "lsreshape:export",
        "expr": "lsreshape:expr",
        "valueExpr": "lsreshape:valueExpr",
        "ifEmpty": "lsreshape:ifempty",
        "joinMethod": "lsreshape:joinMethod",
        "joinDelimiter": "lsreshape:joinDelimiter"
    }
}
